# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

# trigger:
# - none
# - master

trigger:
  branches:
    include:
    - build

variables:

  ### Variables ###
  serviceNamespace: '$(SERVICE_NAMESPACE)'
  serviceName: $(SERVICE_NAME)

pool:
  vmImage: ubuntu-latest

stages:

  #### Build Image Stage ####
  - stage: Deploy_Image
    jobs:
      - job: Deploy_Image
        steps:
        - bash: |
            set -x # Echo Commands
            # Kubectl Login
            # Deploy Image
            # kubectl -n ${serviceNamespace} apply -f ../../kubernetes-manifests/${serviceName}.yaml
          displayName: 'Deploying Image...'